# Frontend (Angular)

## 開発サーバー
ローカルで起動:
```bash
ng serve
```

起動後に `http://localhost:4200/` にアクセスしてください。ソース変更は自動で反映されます。

## ビルド
```bash
ng build
```

生成物は `dist/` に出力されます。

## テスト
```bash
ng test
```

Karma による単体テストを実行します。
